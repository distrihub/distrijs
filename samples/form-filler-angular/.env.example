# Server-side only — read by proxy.conf.cjs in the Node dev-server process.
# NEVER sent to the browser; the dev server exchanges it for a short-lived
# access token via POST {DISTRI_BASE_URL}/token.
DISTRI_API_KEY=
DISTRI_BASE_URL=https://api.distri.dev/v1
